# 安全政策

本项目会接触**校园网账号密码**和**学校业务系统的登录 Cookie**，所以安全问题请按下面的方式处理。

## 报告漏洞

**请不要开公开 issue 描述可利用细节。**

- 首选：在仓库的 Security 标签页用 GitHub 的 **Private vulnerability reporting**（私有漏洞报告）提交。
  如果这个入口还没打开，说明仓库管理员还没启用（Settings → Security → Private vulnerability reporting），
  请改用下面第二种方式，并在 issue 里只写「有一个安全问题，已私信」而不写细节。
- 备选：通过 GitHub 组织 [SzuDesktopTeam](https://github.com/SzuDesktopTeam) 联系维护者。

请在报告里写清：受影响的版本或提交、复现步骤、影响范围（本机 / 校园网 / 学校系统）、
以及你是否已经公开过这个细节。

## 本项目对凭据的处理（现状，如实）

| 平台 | 校园网密码 | 业务 Cookie（教务 / 成绩 / 课表） |
|---|---|---|
| Windows | DPAPI 加密后落盘，换机器或换用户解不开 | 同样走 DPAPI |
| macOS | 系统钥匙串（`security`）；写入前用一次性条目自检，失败明确报错，不退回把密码写进命令行参数 | 系统钥匙串 |
| Linux | Secret Service（`secret-tool`）；**没有密钥环时拒绝保存并报错**，不退回明文文件 | 没有密钥环时拒绝保存 |

其他约定：

- 本地服务只监听 `127.0.0.1`，非回环地址直接拒绝启动；所有 `/api/*` 校验 Host、`Sec-Fetch-Site` 与同源 `Origin`。
- 界面不提供代提交入口：预约、选课、付款、签到都由用户在学校官方系统完成。
- 公告、校历、场地空位只读取学校**公开**接口，不做任意网址代理。
- 庭院存档导出不含校园网账号密码。
- 没有 telemetry、没有埋点。

## 已知未解决的安全相关项

这些都记在 [docs/STATUS.md](docs/STATUS.md)，不要当成已修：

- **F06 / F07 / F08**：实验 VPN 模块存在假成功、缺全链路超时、跳过证书验证的问题。
  该模块**不在任何发布件里**（只有 `-tags campusvpn` 才编译），但问题本身没修。
- **F11**：实验 VPN 模块的来源与授权尚未核实。
- **F21 / F25 / F26**：macOS 保存凭据改成标准输入喂入后，CI 的 macOS 探针在真机上发现
  `security -w` 会问两遍（密码 + 确认），而 beta0.7.1 与 beta0.7.2 只喂一行，
  导致**那两个版本的 macOS 命令行版存不了凭据**（F26）。失败是明确的、不泄漏也不落明文，
  但功能不可用。已改为喂两行，**修复尚未发布**；探针不再忽略失败，并已成为发布前置条件。
- 宿舍区 Dr.COM 认证走**明文 HTTP**（`http://172.30.255.42`），这是学校协议的现状、不是本项目的选择。

## 不算漏洞的设计决定

以下是有意为之，报告前请先确认：

- 不使用代码签名证书，因此杀毒软件可能误报（README 已如实说明）。
- 不做自动重连、不做后台周期性认证。
- 读不到状态时报「状态未知」而不是「未开启」——宁可说不知道，也不猜。
- 探测网络时（判区、协议指纹）会跳过证书校验，但这些请求**不携带任何凭据**；
  真正带密码的登录请求保留证书校验。
