// Decorative inventory icons; every control keeps its visible text label.
export const pixelIcon=(id,cls='item-icon')=>`<svg class="sprite ${cls}" aria-hidden="true" focusable="false"><use href="#${id}"></use></svg>`;
